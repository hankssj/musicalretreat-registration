<% if Instrument.find(Evaluation.find(contact.object_id).instrument_id).instrument_type == "string" %>
Contact Eileen
<% elsif Instrument.find(Evaluation.find(contact.object_id).instrument_id).instrument_type == "vocal" %>
Contact Jason
<% else %>
Contact Dan
<% end %>
