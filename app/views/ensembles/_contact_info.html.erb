<% if Instrument.find(Evaluation.find(contact.id).instrument_id).instrument_type == "string" %>
Contact Eileen
<% elsif Instrument.find(Evaluation.find(contact.id).instrument_id).instrument_type == "vocal" %>
Contact Jason
<% else %>
Contact Dan
<% end %>
