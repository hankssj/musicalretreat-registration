<% if ensemble_primary.registration.instrument.large_ensemble == "band_or_orchestra" %>

<table class="eval-table">
  <tr><th colspan="3">Ensemble Choice</th></tr>
  <tr>
    <td>Either Symphonic Band or Festival Orchestra</td>
    <td>Prefer Symphonic Band</td>
    <td>Prefer Festival Orchestra</td>
  </tr>
  <tr>
    <td><%= form.radio_button :large_ensemble_choice, 1, {:checked => true} %></td>
    <td><%= form.radio_button :large_ensemble_choice, 2 %></td>
    <td><%= form.radio_button :large_ensemble_choice, 3 %></td>
  </tr>
</table>

<% elsif ensemble_primary.registration.instrument.large_ensemble == "band" %>

<table class="eval-table">
  <tr><th colspan="1">Ensemble Choice</th></tr>
  <tr><td>Symphonic Band </td></tr>
  <tr><td><%= form.radio_button :large_ensemble_choice, 2, {:checked => true} %></td></tr>
</table>

<% elsif ensemble_primary.registration.instrument.large_ensemble == "festival_or_string_orchestra" %>

<p>
<b>2015 Orchestra Repertoire</b>
<ul>
<li> String Orchestra will play Mendelssohn, <i>Symphony #12</i>
<li> Full Orchestra will play Elgar, <i>The Wand of Youth</i>
</ul>
</p>

<table class="eval-table">
  <tr><th colspan="3">Ensemble Choice</th></tr>
  <tr>
    <td>Festival Orchestra </td>
    <td>String Orchestra </td>
    <td>Either Festival or String Orchestra </td>
  <tr>
    <td><%= form.radio_button :large_ensemble_choice, 3, {:checked => true} %></td>
    <td><%= form.radio_button :large_ensemble_choice, 5 %></td>
    <td><%= form.radio_button :large_ensemble_choice, 6 %></td>
  </tr>
</table>

<% elsif ensemble_primary.registration.instrument.large_ensemble == "chorus" %>
<table class="eval-table">
  <tr><th colspan="1">Ensemble Choice</th></tr>
  <tr><td>Festival Chorus </td></tr>
  <tr><td><%= form.radio_button :large_ensemble_choice, 4, {:checked => true} %></td></tr>
</table>

<% elsif ensemble_primary.registration.instrument.large_ensemble == "none" %>

<table class="eval-table">
<tr><th>Ensemble Options</tr></tr>
<tr><td>
<table>
<tr><td style="text-align left;">I understand that I will not be assigned a Morning Large Ensemble</td><td><%= form.radio_button :large_ensemble_choice, -1, {:checked => true} %></td></tr>
<tr><td style="text-align left;">I would like to sing in the Festival Chorus</td><td><%= form.check_box :want_sing_in_chorus %></td></tr>
<tr><td style="text-align left;">I would like to talk to a faculty member about playing percussion in Symphonic Band</td><td><%= form.check_box :want_percussion_in_band %></td></tr>
</table>
</table>
<% end %>

<div class="toggled-morning-ensemble-options">
  <p>
<%= render :partial => "ensembles/part_large_ensemble", :locals => {:form => form, :instrument => ensemble_primary.registration.instrument} %>
  </p>
</div>

